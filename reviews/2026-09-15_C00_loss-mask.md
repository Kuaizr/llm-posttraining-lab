# 测验与代码审查：2026-09-15 / C00 工具 SFT loss mask

## 1. 本章验收范围

- 主计划对应条目：C00 最小训练能力检查；借用 C03 的 token、assistant mask 与 label 正确性标准。
- 待审代码/报告/实验：`scripts/inspect_tokens.py`、一条固定 Qwen3.5-0.8B 工具轨迹。
- 审查文件快照：2026-09-15 工作树；尚未提交。
- 非本次范围：模型权重加载、GPU backward/optimizer、训练效果、完整 C03。

## 2. 测验记录

| 题号/类型 | 问题 | 原始回答/记录路径 | 提示等级 | 评分 0～4 | 主要缺口与变式复查 |
|---|---|---|---:|---:|---|
| 1 概念 | system/user/assistant/tool 哪些区域参与工具 SFT loss | “assistant 和 tool是要算loss的其他设置为-100” | 无；答后明确答案 | 2 | 错把环境 observation 当作模型动作；原题已看答案 |
| 2 即时确认 | 新工具轨迹中哪些内容参与 loss | “B” | 答案已出现在同页短讲 | 不计分 | 仅确认读懂，不作为独立掌握证据 |
| 3 迁移/边界 | 哪条 validator 条件只是本检查样例要求，不是所有合法 SFT 样本的通用规则 | “D” | 无 | 4 | 独立识别“每条样本同时含 0/1”不是普遍数学要求 |

- 当前计分：6/8；即时确认题不计分，尚不换算为章节总分。
- mask 核心点：tool observation 的监督边界经明确答案后已理解，并通过边界变式题；后续仍需在新数据上复查。
- 已看答案：第 1 题的正确角色范围和第 2 题均已看答案，不能作为独立掌握证据。

## 3. 审查发现

| 编号 | 严重性 | 文件:行号 | 问题与影响 | 最小复现/测试 | 最小修复建议 | 当前状态 |
|---|---|---|---|---|---|---|
| C00-M1 | 重要 | `scripts/inspect_tokens.py:51` | `validate_labels` 保留 `NotImplementedError`，脚本无法到达检查输出 | 静态读取完整函数 | 实现长度、mask 域、有效 label 和逐位置映射检查 | Codex 演示修复 |
| C00-M2 | 重要 | `scripts/inspect_tokens.py:39` | mask 较短时被静默接受，较长时产生不明确的 `IndexError` | 隔离调用长短不一致输入 | 在索引前检查长度，并拒绝非 0/1 mask 值 | Codex 演示修复 |

## 4. 执行证据

| 测试/命令 | 实际运行状态 | 输出/日志路径 | 结论 |
|---|---|---|---|
| AST 隔离执行 `build_labels`/`validate_labels`，覆盖正常、长短 mask、非法值和错误 label | 已执行 | 本次终端输出；代码见 `scripts/inspect_tokens.py` | 所有预期检查 PASS |
| `source scripts/env.sh && HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 python -u scripts/inspect_tokens.py` | 已执行 | 本次终端输出 | 97 token；50 个有效 label；工具调用和最终回答受监督，tool response 全部 mask；退出码 0 |
| 模型权重加载、GPU backward/optimizer | NOT RUN | 无 | 不在本次授权范围 |

- 是否擅自修改源码/测试：学习者先实现 `build_labels` 正常路径；学习者选择 A 后，Codex 获准演示并修改长度/非法值保护及完整 validator。
- 是否修改 oracle、测试预期或失败分母：否。

## 5. 学习者修复与独立解释

- 学习者独立完成：使用显式 `for` 循环实现 mask=1 时复制 token ID、否则保留 `-100` 的正常路径。
- Codex 提供的实现或补丁：长度保护、非法 mask 拒绝、`validate_labels` 四类断言及回归检查。
- 新增变式题：区分固定检查样本的验收条件与通用训练不变量。
- 学习者能否不看答案完成：正常 label 映射可以；validator 的 Python 表达尚不能，已看完整实现；边界变式题独立正确。
- 回归测试结果：隔离测试 PASS；本地 Qwen tokenizer/processor 端到端检查 PASS。

## 6. 分开给出结论

- 学习状态：LEARNING。
- 工程执行：SMOKE_ONLY。
- 效果证据：NOT_EVALUATED。
- 支持上述结论的证据路径：`scripts/inspect_tokens.py`、`notes/C00_loss_mask.md`、本文件。
- 阻断项/待算力验证事项：真实 Qwen backward/optimizer 尚未授权与执行。
- 必须复习的一个知识点：在新的多轮轨迹中独立区分 assistant action 与环境 observation 的监督边界。
- 下一次唯一任务：确认 Qwen3.5-0.8B 单样本 LoRA 单步 GPU optimizer smoke 的配置、预算与授权。
- `PROGRESS.md` 更新说明：登记 mask 实测、帮助等级、代码归属和下一次唯一任务。
