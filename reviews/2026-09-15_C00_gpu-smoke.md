# 测验与代码审查：2026-09-15 / C00 真实 Qwen GPU smoke

## 1. 本章验收范围

- 主计划对应条目：C00 的真实小模型 forward/backward、LoRA 参数更新、保存重载和证据边界。
- 待审代码/报告/实验：`scripts/smoke_qwen_lora_step.py`、`reports/C00_qwen_lora_step.md`。
- 审查 commit：`17a685d run Qwen LoRA optimizer smoke`。
- 非本次范围：多步训练、收敛、任务效果、2B 训练、DPO/GRPO/PPO。

## 2. 测验记录

| 题号/类型 | 问题 | 原始回答/记录路径 | 提示等级 | 评分 0～4 | 主要缺口与变式复查 |
|---|---|---|---:|---:|---|
| 1 证据边界 | 单次 optimizer step 成功能够证明什么 | “C”——当前模型、mask、LoRA、CUDA 链路能完成一次真实参数更新和保存重载 | 无 | 3 | 选择正确；未用自己的话展开为何不能外推到收敛、效果和更大训练配置 |
| 2 实验设计 | 要证明 SFT 后任务成功率提升，哪个最小方案有效 | “C”——固定父模型、解码和预算，在未训练 dev 上做前后 task-success 对照并保留失败分母 | 无 | 3 | 独立选择正确；后续需真正定义数据切分和指标实现 |

- 当前计分：6/8；本次只验收单步 smoke 的证据边界，不换算为完整章节总分。
- 核心点：没有把参数变化、loss 有限或单样本训练误写成效果提升。
- 已看答案：两题均在作答后才给反馈，未提前看答案。

## 3. 审查发现

| 编号 | 严重性 | 文件:行号 | 问题与影响 | 最小复现/测试 | 最小修复建议 | 当前状态 |
|---|---|---|---|---|---|---|
| C00-G1 | 建议 | `scripts/smoke_qwen_lora_step.py:147` | 186 个张量有非零梯度，但 AdamW step 后 372 个张量变化；若只看 changed count 会把 weight decay 造成的变化误认为梯度学习信号 | 分开统计 nonzero gradient 与 changed parameter tensor | 报告两项并明确解释差异 | 已在报告说明 |

未发现阻断或重要问题。脚本只含一次 `backward()` 和一次 `optimizer.step()`，且对已有输出目录、全 mask、非有限 loss/gradient、零梯度、无参数变化和 reload checksum 不一致均会停止。

## 4. 执行证据

| 测试/命令 | 实际运行状态 | 输出/日志路径 | 结论 |
|---|---|---|---|
| AST 静态检查 backward/optimizer step 调用数 | 已执行 | 本次终端输出 | 各恰好 1 次 |
| 10 分钟硬超时、强制离线的 Qwen3.5-0.8B LoRA smoke | 已执行 | `reports/C00_qwen_lora_step.md` | loss/gradient finite；参数更新；adapter reload PASS；峰值 2387/2442MiB |
| adapter 文件与 SHA-256 核验 | 已执行 | Git 忽略的 `runs/C00-QWEN-LORA-STEP-001/adapter/` | safetensors 10,871,936 bytes；hash 已记录 |
| 第二次 optimizer step、生成评测、效果实验 | NOT RUN | 无 | 超出授权和本次目标 |

- 是否擅自修改源码/测试：否；学习者明确授权单步 GPU smoke 后由 Codex 实现和执行。
- 是否修改 oracle、测试预期或失败分母：否。

## 5. 学习者修复与独立解释

- 学习者独立修复：本次训练脚本由 Codex 实现，无学习者代码修复。
- Codex 提供的实现或补丁：离线单步脚本、停止条件、LoRA 范围、checksum 和实验报告。
- 新增变式题：工程能力证据与效果证据；最小训练前后独立评测方案。
- 学习者能否不看答案完成：两道选择题均独立正确；尚未独立实现实验配置或用自由回答展开边界。
- 回归测试结果：单步 smoke PASS；Git 工作树将在本记录提交后保持同步。

## 6. 分开给出结论

- 学习状态：LEARNING。
- 工程执行：SMOKE_ONLY。
- 效果证据：NOT_EVALUATED。
- 支持上述结论的证据路径：`scripts/smoke_qwen_lora_step.py`、`reports/C00_qwen_lora_step.md`、本文件。
- 阻断项/待算力验证事项：无；优化 kernel 非当前前置条件。
- 必须复习的一个知识点：GRPO 同组全同 reward 与 advantage 退化。
- 下一次唯一任务：开始 C00 综合收尾复查，一次一道选择题；第一题复查 GRPO 同组 advantage。
- `PROGRESS.md` 更新说明：登记两题、区分工程与效果证据，并关闭本次单步 GPU 授权。
