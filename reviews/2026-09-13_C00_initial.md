# 测验与代码审查：2026-09-13 / C00 初测

## 1. 本章验收范围

- 主计划对应条目：C00 后训练地图、数据类型、环境体检。
- 待审代码/报告/实验：尚无学习者代码；`notes/C00_map.md` 待完成。
- 审查 commit 或文件快照：当前目录不是 Git 仓库，无 commit 可引用。
- 非本次范围：依赖安装、模型下载、CUDA 训练、C01 及后续章节。

## 2. 测验记录

一次一题完成初测；以下保留学习者首次回答及后续提示情况。

| 题号/类型 | 问题 | 原始回答 | 提示等级 | 评分 0～4 | 主要缺口与变式复查 |
|---|---|---|---|---:|---|
| 1 概念 | GRPO 中 prompt、在线 completion、reward/advantage 的关系 | 正确指出 prompt 预先存在、4 个 completion 在线产生；经概念提示给出 advantage 符号 `[+,+,-,-]` | 概念提示 | 3 | 需主动说明 advantage 大小、组内基线与全同奖励边界 |
| 2 概念 | 有 500 条正确专家轨迹但无偏好对和可靠 reward 时如何选 SFT/DPO/GRPO | 独立选择 SFT；最初把“有多种正负轨迹”同时当作 DPO/GRPO 条件，经提示后区分固定偏好对与在线 rollout | 概念提示 | 2 | 变式复查 DPO 固定 chosen/rejected 与 GRPO 当前策略采样 |
| 3 手算/数据流 | 两个 prompt 的奖励应如何做组内 advantage | 正确算出 A `[0.5,-0.5]`、B `[1,-1]`；未识别全局均值会让组内较差样本也获得正信号 | 明确讲解 | 2 | 换不同奖励尺度复查，不能沿用原题 |
| 4 排错 | `nvidia-smi` 可用、无 `nvcc`、无 torch 是否证明 PyTorch CUDA 不可用 | 知道 `nvidia-smi` 主要检查驱动、`torch.cuda.is_available()` 是关键 API；不了解 `nvcc` 与实际 kernel 测试 | 明确答案 | 1 | 安装获授权后，用新配置独立设计 CUDA tensor forward/backward/synchronize smoke test |
| 5 迁移 | SFT 后非法调用下降、可靠 verifier 且 rollout 有成败差异时如何继续及如何评测 | 独立选择 GRPO 并指出成功信号与成本可控；无法定义训练 reward 之外的评测指标 | 明确答案 | 2 | 复查 task success、invalid tool call rate 的分子分母及失败计入方式 |

- 总分：10 / 20。
- 数据泄漏、mask、reward 等核心点是否有重大缺口：尚未检查数据泄漏；causal objective 原理理解基本正确，但需核验具体框架的 shift/mask 契约；reward 的组内归一化和独立评测仍需补学。
- 哪些题看过答案，需要换题重测：第 3、4、5 题已看明确答案；第 1、2 题使用过概念提示，均需变式复查。

## 3. 审查发现

| 编号 | 严重性 | 文件:行号 | 问题与影响 | 最小复现/测试 | 最小修复建议 | 当前状态 |
|---|---|---|---|---|---|---|
| — | — | — | 尚无学习者代码可审查 | NOT RUN | 完成 `notes/C00_map.md` 后审查 | 待提交 |

## 4. 执行证据

| 测试/命令 | 实际运行状态 | 输出/日志路径 | 结论 |
|---|---|---|---|
| `uname -a`、读取 `/etc/os-release`、`df -h .` | 已运行 | `PROGRESS.md` 环境表 | Arch Linux on WSL2；C 盘当时剩余约 75 GB |
| `python --version` 与 `importlib.metadata` 包版本查询 | 已运行 | `PROGRESS.md` 环境表 | Python 3.14.5；当前 Python 无 pip 与训练栈 |
| `nvidia-smi`、`nvcc --version` | 已运行 | `PROGRESS.md` 环境表 | RTX 5070 Ti 可见；无 `nvcc`；尚不能证明 PyTorch CUDA 可用 |
| CUDA tensor / forward / backward / save-reload | NOT RUN | 无 | PyTorch 和模型均未安装，且未获安装/下载/GPU 训练授权 |
| 将仓库复制到 `/mnt/d/llm-posttraining-lab` 并逐文件执行 SHA-256 diff | 已运行 | D 盘仓库；命令返回 diff 为空 | D 盘副本与 C 盘源文件内容一致；C 盘源副本保留 |

- 未运行内容及原因：所有 PyTorch、模型与 GPU smoke test 均未运行；当前没有依赖，且安装、下载、训练未授权。
- 是否擅自修改源码/测试：否；仅按用户授权迁移仓库并更新教学记录。
- 是否修改了 oracle、测试预期或失败分母：否。

## 5. 学习者修复与独立解释

- 学习者独立修复：尚无代码修复。
- Codex 提供的实现或补丁：未提供核心作业实现；仅创建教师审查记录并更新 `PROGRESS.md`。
- 新增临时需求/变式题：后续需对数据契约、组内 advantage、CUDA 分层检查和独立评测做变式复查。
- 学习者能否不看答案完成：未验证。
- 回归测试结果：NOT RUN。

## 6. 分开给出结论

- 学习状态：LEARNING。
- 工程执行：NOT_RUN。
- 效果证据：NOT_EVALUATED。
- 支持上述结论的证据路径：`reviews/2026-09-13_C00_initial.md`、`PROGRESS.md`。
- 阻断项/待算力验证事项：当前无学习阻断；训练环境、PyTorch CUDA 和模型 smoke test 尚未建立。
- 必须复习的一个知识点：训练内 reward/advantage 与独立任务评测指标的区别。
- 下一次唯一任务：学习者独立完成 `notes/C00_map.md` 的 SFT/DPO/GRPO 对照表。
- `PROGRESS.md` 更新说明：迁移完成后以 D 盘副本为准，登记当前状态、薄弱点和下一步。
